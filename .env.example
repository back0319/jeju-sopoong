# Supabase 프로젝트 Settings → API에서 확인 (로컬: supabase status)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Supabase Settings → API → service_role. 서버 전용이며 공개 변수로 만들지 않습니다.
SUPABASE_SERVICE_ROLE_KEY=
# 내부 테스트에서는 true. 확정 동의문을 등록한 뒤 실제 운영에서 false로 변경합니다.
INTERNAL_TEST_MODE=true
